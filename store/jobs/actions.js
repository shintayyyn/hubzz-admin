import * as jobsApi from '@/api/jobs'

export default{
    async initializeAdminJobListener({ state, commit, dispatch }) {

    },
//     async fetchLocumJobs({ commit }, payload){
//         const response = await jobsApi.fetchLocumJobs(this.$axios, payload)
//     }
}